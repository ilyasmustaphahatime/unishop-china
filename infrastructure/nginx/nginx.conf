events {}
http { server { listen 80; add_header Referrer-Policy "no-referrer" always; location /api/ { proxy_hide_header Referrer-Policy; proxy_pass http://backend:8000; } location / { proxy_pass http://frontend:5173; } } }
